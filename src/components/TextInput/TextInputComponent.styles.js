import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    width: '100%',
    color: 'lightblue',
    fontSize: 20,
    padding: 20,
    fontWeight: 'bold',
    borderWidth: 1,
    borderColor: 'black',
  },
});

export default styles;
